# Copy this file to .env.local and fill in real values (see supabase.ts
# for how these are consumed). .env.local is gitignored; this file is not.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
